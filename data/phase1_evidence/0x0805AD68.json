{
  "address": "0x0805AD68",
  "attempts": [
    {
      "number": 1,
      "note": "natural C from the ROM body; first attempt",
      "source": "src/world/p1_5ad68.c",
      "sha256": "2a39850ea8e27bc6cb381113b8d8bac7edaacc6d0c227f9c139e85173516e67f",
      "compiled_size": 86,
      "mapped_size": 86,
      "same": 37,
      "total": 42,
      "matching": false,
      "code": "/* Yield the group members, or the single object, in order — 0x0805AD68.\n * Phase 1; compared against the ROM twins. Measurement: data/phase1_evidence/0x0805AD68.json. */\n#include \"gba_types.h\"\n#include \"phase1_types.h\"\n\nextern Phase1GroupedActor *ResolveRecordNodeChain(u16 id);\nu32 FUN_0805ad68(Phase1GroupedActor *actor,s32 *cursor,Phase1GroupedActor **out)\n{\n    Phase1RecordGroup *group = actor->group;\n    if (actor == 0) return 0;\n    if (group->count == 0) {\n        if (*cursor == 0) {\n            *cursor = 1;\n            *out = actor;\n            return 1;\n        }\n        *out = 0;\n        return 0;\n    }\n    if (*cursor >= group->count) {\n        *out = 0;\n        return 0;\n    }\n    *out = ResolveRecordNodeChain(group->ids[*cursor]);\n    (*cursor)++;\n    return 1;\n}\n"
    },
    {
      "number": 2,
      "note": "make the last branch a single-exit if/else; keep the sibling's call target",
      "source": "src/world/p1_5ad68.c",
      "sha256": "67514772eed18041fd183bd93f1ad47a9e01d8bc8f86912dc5fedeae2e067b3a",
      "compiled_size": 86,
      "mapped_size": 86,
      "same": 42,
      "total": 42,
      "matching": true,
      "code": "/* Yield the group members, or the single object, in order — 0x0805AD68.\n * Phase 1; compared against the ROM twins. Measurement: data/phase1_evidence/0x0805AD68.json. */\n#include \"gba_types.h\"\n#include \"phase1_types.h\"\n\nextern Phase1GroupedActor *GetOrCreateRecordNode(u16 id);\nu32 FUN_0805ad68(Phase1GroupedActor *actor,s32 *cursor,Phase1GroupedActor **out)\n{\n    u32 result;\n    Phase1RecordGroup *group = actor->group;\n    if (actor == 0) return 0;\n    if (group->count == 0) {\n        if (*cursor == 0) {\n            *cursor = 1;\n            *out = actor;\n            return 1;\n        }\n        *out = 0;\n        return 0;\n    }\n    if (*cursor >= group->count) {\n        *out = 0;\n        result = 0;\n    } else {\n        *out = GetOrCreateRecordNode(group->ids[*cursor]);\n        (*cursor)++;\n        result = 1;\n    }\n    return result;\n}\n"
    }
  ]
}
