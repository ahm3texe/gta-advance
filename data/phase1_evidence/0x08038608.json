{
  "address": "0x08038608",
  "attempts": [
    {
      "number": 1,
      "note": "natural C from the ROM body; first attempt",
      "source": "src/world/p1_38608.c",
      "sha256": "e1ff912068cc6973acb42e29a0b764cca36b746432f67399a53b9604b70e1fa7",
      "compiled_size": 60,
      "mapped_size": 60,
      "same": 23,
      "total": 29,
      "matching": false,
      "code": "/* Pick the alternative model with a one-in-four chance — 0x08038608.\n * Phase 1; compared against the ROM twins. Measurement: data/phase1_evidence/0x08038608.json. */\n#include \"gba_types.h\"\n#include \"phase1_types.h\"\n\nextern const Phase1Model gRom08CA7E64[];\nextern u32 FUN_08032548(void);\nconst Phase1Model * FUN_08038608(u32 index)\n{\n    const Phase1Model *model = &gRom08CA7E64[index];\n    if (model->alternate != 0 && (FUN_08032548() & 3) == 0)\n        index = model->alternate;\n    return &gRom08CA7E64[index];\n}\n"
    },
    {
      "number": 2,
      "note": "take the table base as a separate expression before the index computation",
      "source": "src/world/p1_38608.c",
      "sha256": "49aacd1a51b10bbecb1abc0f0a36c1b1ad6987a810a15344a87286b4c6b6f0d1",
      "compiled_size": 60,
      "mapped_size": 60,
      "same": 27,
      "total": 29,
      "matching": false,
      "code": "/* Pick the alternative model with a one-in-four chance — 0x08038608.\n * Phase 1; compared against the ROM twins. Measurement: data/phase1_evidence/0x08038608.json. */\n#include \"gba_types.h\"\n#include \"phase1_types.h\"\n\nextern const Phase1Model gRom08CA7E64[];\nextern u32 FUN_08032548(void);\nconst Phase1Model * FUN_08038608(u32 index)\n{\n    const Phase1Model *model = gRom08CA7E64;\n    model += index;\n    if (model->alternate != 0 && (FUN_08032548() & 3) == 0)\n        index = model->alternate;\n    return &gRom08CA7E64[index];\n}\n"
    },
    {
      "number": 3,
      "note": "express the model table access through a natural inline helper",
      "source": "src/world/p1_38608.c",
      "sha256": "58b3c4561f5603001fdefa9edd7150f3c7db2704bd9fba106d62ef9c7dd33be9",
      "compiled_size": 60,
      "mapped_size": 60,
      "same": 23,
      "total": 29,
      "matching": false,
      "code": "/* Pick the alternative model with a one-in-four chance — 0x08038608.\n * Phase 1; compared against the ROM twins. Measurement: data/phase1_evidence/0x08038608.json. */\n#include \"gba_types.h\"\n#include \"phase1_types.h\"\n\nextern const Phase1Model gRom08CA7E64[];\nextern u32 FUN_08032548(void);\nstatic inline const Phase1Model *LookupModel(u32 index)\n{ return &gRom08CA7E64[index]; }\n\nconst Phase1Model * FUN_08038608(u32 index)\n{\n    const Phase1Model *model = LookupModel(index);\n    if (model->alternate != 0 && (FUN_08032548() & 3) == 0)\n        index = model->alternate;\n    return LookupModel(index);\n}\n"
    },
    {
      "number": 4,
      "note": "move the inline table base into a separate local; final attempt",
      "source": "src/world/p1_38608.c",
      "sha256": "dff687a70d2a03a8d2617443da464a4bad97513a01e5cb44fc81dae214dbeb32",
      "compiled_size": 60,
      "mapped_size": 60,
      "same": 28,
      "total": 29,
      "matching": false,
      "code": "/* Pick the alternative model with a one-in-four chance — 0x08038608.\n * Phase 1; compared against the ROM twins. Measurement: data/phase1_evidence/0x08038608.json. */\n#include \"gba_types.h\"\n#include \"phase1_types.h\"\n\nextern const Phase1Model gRom08CA7E64[];\nextern u32 FUN_08032548(void);\nstatic inline const Phase1Model *LookupModel(u32 index)\n{\n    const Phase1Model *table = gRom08CA7E64;\n    return table + index;\n}\n\nconst Phase1Model * FUN_08038608(u32 index)\n{\n    const Phase1Model *model = LookupModel(index);\n    if (model->alternate != 0 && (FUN_08032548() & 3) == 0)\n        index = model->alternate;\n    return LookupModel(index);\n}\n"
    }
  ]
}
